# Volumes
add volumes in compose files and test
```
docker conpose up -d
```
# 1. USe minimal image which alpine or slim images
# 2. Multi Stage Builds:
# 3. Run conatianer as non-root user
# 4. Layer Caching - frequently changing indtruction keep it in bottom of the dockerfile
    ```
    always combine multiple RUN instruction into i=one instruction
```
